# 后续路线与待办

## 1. 当前阶段判断

当前项目进度可以定义为：

- 聊天：完成
- 固定文档知识库：完成
- 向量检索知识库：已接入，待稳定
- 真正向量库：未开始

## 2. 建议的下一步优先级

### 第一优先级：把第二阶段跑稳定

要先解决：

- `/api/ai/rag` 稳定返回
- 大文档场景不要立即失败
- 配置修改能明确感知

建议动作：

1. 用小文档集测试向量检索
2. 把大文档拆分
3. 把错误信息继续显式化
4. 观察第一次构建向量缓存和后续查询的差异

### 第二优先级：把文档组织好

建议把知识库内容按主题拆分：

- 接口说明
- 字段说明
- 页面规则
- 业务流程
- 测试标记

### 第三优先级：做持久化索引

当前 embedding 缓存只在内存里，后端重启后会丢失。

建议后续做：

- 本地文件缓存 embedding
- 或直接接入真正的向量数据库

## 3. 真正向量库阶段建议

后续可选方向：

- `Qdrant`
- `PostgreSQL + pgvector`
- `Milvus`
- `Elasticsearch / OpenSearch`

更实际的推进顺序：

1. 本地文件缓存 embedding
2. 向量检索稳定
3. 再迁移到正式向量库

## 4. 后续功能清单

后面可以继续补：

- 知识库文件列表接口
- 当前扫描到的文档调试接口
- 重新索引接口
- 单文件增量索引
- 文档上传
- 文档删除
- chunk 查看
- chunk 命中明细
- Prompt 调试输出

## 5. 文档维护建议

以后每推进一阶段，都同步更新这组文档：

- `01` 负责写当前状态
- `02` 负责写接口和页面变化
- `03` 负责写知识库目录规则
- `04` 负责写新增故障与排障方式
- `05` 负责写后续路线

这样后面就不需要再从聊天记录里倒推项目现状。
